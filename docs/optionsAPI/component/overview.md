# 组件系统

```txt
           ┌──────► 组件基础(index)
           │
           ├──────► 组件状态(data)
           │
           ├──────► 组件方法(methods)
           │
           ├──────► 计算属性(computed)
           │
Components─┼──────► 侦听器(watch)
           │
           ├──────► 插槽(slot)
           │
           ├──────► 动效(effect)
           │
           ├──────► 组件通信(communicate)
           │
           └──────► 生命周期(lifecycle)
```
